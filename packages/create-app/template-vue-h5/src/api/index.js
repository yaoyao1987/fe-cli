import request from '@/utils/request'

/**
 * example
 *
export async function accountLogin (params) {
  return request({
    url: '/customers/passport/authorize',
    method: 'POST',
    data: params
  })
}
 */
